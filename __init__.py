"""Tools for pre- and post-processing HydroGeoSphere (HGS) simulations."""

# pdoc honours `__docformat__` from the root module being documented, and
# applies it to the whole package tree. The submodules' docstrings are written
# in NumPy style, so declare it here as well (the per-module declarations only
# take effect when a submodule is documented as its own root).
__docformat__ = 'numpy'
